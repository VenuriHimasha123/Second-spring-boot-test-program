# Backend for simple spring boot program

Simple Spring Boot demo project that exposes a few REST endpoints.

## Prerequisites
- Java JDK 17
- Maven 3.6+
- Windows (commands below assume PowerShell or CMD)

## Build

From project root (`D:\test_icet_springboot\icetdemoo`):

PowerShell / CMD:
```bash
mvn clean package
